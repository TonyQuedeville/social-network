/*import { useHistory } from 'react-router-dom';

function useRedirectToLogout() {
    const history = useHistory();

    const redirectToLogout = () => {
        history.push('/');
    }

    return redirectToLogout;
}

export default useRedirectToLogout;
//*/
